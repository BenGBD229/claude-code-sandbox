# Copy to .env and fill in as needed.

# Only needed in sandboxes where Remotion can't download its own Chrome
# Headless Shell (network egress blocked). Point this at a preinstalled
# Chromium/Chrome-Headless-Shell binary. Leave unset otherwise.
# REMOTION_BROWSER_EXECUTABLE=/path/to/chrome-headless-shell
